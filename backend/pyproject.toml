[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "C4", "SIM"]
ignore = ["B008", "E501"]

[tool.ruff.lint.isort]
known-first-party = ["recommender", "routers"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
